import React from "react";
import NewsletterSignup from "@/app/_components/Newsletter";
export const Newsletter = ({ title, description }) => {
    return <NewsletterSignup title={title} description={description} />;
};
