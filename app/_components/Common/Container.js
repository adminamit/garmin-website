import React from "react";

const Container = ({ children }) => {
    return (
        <div data-v-d3acc470="" className="container">
            {children}
        </div>
    );
};

export default Container;
